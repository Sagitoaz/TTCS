# Shared VFX Materials

Store reusable materials used by sprite VFX prefabs.

Naming convention:

- `M_VFX_<Theme>_<Purpose>`
- Example: `M_VFX_BlueSlash_Additive`

Keep common materials here. Character-specific material variants can stay next to that character's VFX prefab if needed.
